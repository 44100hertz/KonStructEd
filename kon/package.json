{
  "name": "kon",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "ls src/* | entr wasm-pack build --dev",
    "build": "wasm-pack build --release"
  },
  "devDependencies": {
    "wasm-pack": "^0.12"
  }
}

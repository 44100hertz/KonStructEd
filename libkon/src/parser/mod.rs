pub mod lexer;
pub mod parser;
pub mod unparser;

//use crate::cst::{Node, Operator};

// pub fn text_to_tree(text: &str) -> Node {
//     let tokens = lexer::lex_tokens(text).unwrap();
//     Node::String(format!("{}\n{:?}", text, tokens))
// }
